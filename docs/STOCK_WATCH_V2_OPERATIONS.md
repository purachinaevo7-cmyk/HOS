# Stock Watch operations

Configure `DISCORD_WEBHOOK_URL` and `HOS_PRIVATE_PROFILE_JSON` as Actions
Secrets. Do not create per-account numeric Secrets or commit runtime artifacts.
If the profile, official IR evidence, price freshness, funding gate, or prior
execution confirmation is missing, the system remains fail-closed.
